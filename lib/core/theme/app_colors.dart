import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const primaryLight = Color(0xFF1A73E8);
  static const primaryDark = Color(0xFF89B4F7);

  // Background colors
  static const backgroundLight = Color(0xFFFFFFFF);
  static const backgroundDark = Color(0xFF121212);

  // Surface colors
  static const surfaceLight = Color(0xFFF8F9FA);
  static const surfaceDark = Color(0xFF1E1E1E);

  // Text colors
  static const textPrimaryLight = Color(0xFF202124);
  static const textPrimaryDark = Color(0xFFE8EAED);
  static const textSecondaryLight = Color(0xFF5F6368);
  static const textSecondaryDark = Color(0xFF9AA0A6);

  // Error colors
  static const errorLight = Color(0xFFB00020);
  static const errorDark = Color(0xFFCF6679);

  // Success colors
  static const successLight = Color(0xFF0F9D58);
  static const successDark = Color(0xFF81C995);
}